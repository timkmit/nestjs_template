<!DOCTYPE html>
<html>
  <head>
    <title>User List</title>
  </head>
  <body>
    <h1>User List</h1>
    <ul>
      {{#each users}}
        <li>ID: {{this.id}}, Name: {{this.name}}</li>
      {{/each}}
    </ul>
  </body>
</html>
